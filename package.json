{
  "name": "express-svg-fallback",
  "version": "0.1.2",
  "description": "Middleware for Express that automatically converts SVG files to PNG format when the request comes from a browser that does not support SVG.",
  "main": "express-svg-fallback.js",
  "scripts": {
    "test": "node ./node_modules/mocha/bin/mocha ./test/*.spec.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/StefanoDalpiaz/express-svg-fallback.git"
  },
  "author": "Stefano Dalpiaz",
  "license": "MIT",
  "dependencies": {
    "mkdirp": "^0.5.0",
    "useragent": "^2.1.5",
    "svg2png": "^1.1.0"
  },
  "devDependencies": {
    "express": "^4.12.2",
    "mocha": "^2.2.1",
    "supertest": "^0.15.0"
  }
}
